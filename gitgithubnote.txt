Git is a Version Control System or Source Code Manager (VCS/SCM).
need to learn
i) Bug fixing and version 
ii) Collaboration
iii) Nonlinear development
git config --global user.name shyamashreeghorai24
git config --global user.email aparnaghorai17@gmail
To check
git config --global user.name
git config --global user.email

Workspace untracked--> Staging tracked--> Local Repo commit--> Remote Repo push
git status
git init
git config --global --add safe.directory "D:/USB Drive/Git_Github"
git add .
git commit -m "this is my first commit"
git push origin main
//after adding if you want to rollback
git restore --staged first.py
//to see the commit
git log
git log --oneline
git show 4771c7c
git checkout 4771c7c (checkout a specific commit)
git checkout master
git log --stat
git log -p
git diff(changes made within staging area before commit)
git branch helper
git branch helper 4771c7c ( to create a branch from any specific commit)
git branch
git checkout helper
git checkout master
git log --oneline --all (to see all the branch with commit)
git log --oneline -all --graph
git branch -D helper
git branch helper2
git checkout master
git marged helper2








